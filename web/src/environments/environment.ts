// `ng build --env=dev` then `environment.ts` will be used instead.

export const environment = {
  production: false,
  base_api_url: "https://api.faturesimples.space",
  base_cep_url: "https://viacep.com.br/ws/{cep}/json/",
};
